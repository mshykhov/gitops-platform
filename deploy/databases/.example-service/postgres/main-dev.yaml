# =============================================================================
# DEV ENVIRONMENT - PostgreSQL
# =============================================================================
cluster:
  instances: 1

  storage:
    size: 1Gi

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

  backup:
    enabled: false
